
```dotenv
# JWT signing secret — change in production
JWT_SECRET=replace_this_with_a_strong_secret

# Access token lifetime (minutes)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Local SQLite DB path (keep the sqlite:///./ prefix when setting in code)
DATABASE_URL=sqlite:///./users.db

# Optional: enable Supabase proxying (leave commented if not using Supabase)
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Failed-login lockout policy (per-email)
# Number of failed attempts before temporary lockout
MAX_FAILED_ATTEMPTS=5
# Lockout duration in seconds (default 180 = 3 minutes)
LOCKOUT_SECONDS=180

# Default test user (local SQLite auth):
# email: test@example.com
# password: password123

# Optional: switch limiter storage to Redis for multi-instance deployments
# Example: RATE_LIMITER_URI=redis://localhost:6379/0
RATE_LIMITER_URI=

# Frontend origin for CORS (comma-separated allowed origins is not supported here; list one origin)
FRONTEND_ORIGIN=http://localhost:3000

# Refresh token lifetime (days) for local auth
REFRESH_TOKEN_EXPIRE_DAYS=7

# Admin token used to protect admin endpoints (set to a strong random value)
ADMIN_TOKEN=replace_with_a_strong_admin_token

